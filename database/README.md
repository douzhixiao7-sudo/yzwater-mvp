# 数据库交付目录

数据库文件按以下顺序组织：

1. `01-schema.sql`：完整结构，包括扩展、表、序列、约束、索引、视图和函数。
2. `02-base-data.sql`：系统运行必需的菜单、角色、权限和字典等基础数据。
3. `03-demo-data.sql`：两个模拟业务案例及其必要关联数据。

生产业务记录、用户资料、Token、日志、短信记录、文件内容和第三方配置不得进入本目录。

当前等待数据库导出文件就绪。导入前需要确认其数据库类型；现有后端业务使用 PostgreSQL/PostGIS。
