<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>yz-module-iot</artifactId>
        <groupId>com.sydigit.yz</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <packaging>jar</packaging>

    <artifactId>yz-module-iot-biz</artifactId>

    <name>${project.artifactId}</name>
    <description>
        物联网 模块，主要实现 产品管理、设备管理、协议管理等功能。
                <!-- TODO 芋艿：后续补充下 -->
    </description>

    <dependencies>
        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-module-system</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-module-iot-core</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-water</artifactId>
            <version>${revision}</version>
        </dependency>

        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-spring-boot-starter-biz-tenant</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.taosdata.jdbc</groupId>
            <artifactId>taos-jdbcdriver</artifactId>
        </dependency>

        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-spring-boot-starter-redis</artifactId>
        </dependency>



        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.sydigit.yz</groupId>
            <artifactId>yz-spring-boot-starter-excel</artifactId>
        </dependency>

        <!-- 消息队列相关 -->
        <!-- TODO @芋艿：临时打开 -->
        <dependency>
            <groupId>org.apache.rocketmq</groupId>
            <artifactId>rocketmq-spring-boot-starter</artifactId>
<!--            <optional>true</optional>-->
        </dependency>
        <dependency>
            <groupId>org.springframework.kafka</groupId>
            <artifactId>spring-kafka</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- IoT 网络组件：接收来自设备的上行数据 -->
        <!--        <dependency>-->
        <!--            <groupId>com.sydigit.yz</groupId>-->
        <!--            <artifactId>yz-module-iot-net-component-http</artifactId>-->
        <!--            <version>${revision}</version>-->
        <!--        </dependency>-->
        <!--        <dependency>-->
        <!--            <groupId>com.sydigit.yz</groupId>-->
        <!--            <artifactId>yz-module-iot-net-component-emqx</artifactId>-->
        <!--            <version>${revision}</version>-->
        <!--        </dependency>-->
    </dependencies>

    <build>
        <!-- 显式打入 mapper XML，避免打包阶段未复制资源导致线上 BindingException: IotDevicePropertyMapper.insert not found -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <includes>
                    <include>**/*</include>
                </includes>
            </resource>
        </resources>
    </build>

</project>
