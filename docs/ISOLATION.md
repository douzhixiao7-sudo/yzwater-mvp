# 第一阶段隔离说明

## 已完成

- 后端、管理后台、移动端已复制到独立目录。
- 未复制 Git 历史、依赖目录、构建目录、日志和旧压缩包。
- 增加后端 `isolated` profile。
- 增加管理后台 `isolated` 环境文件。
- 移动端运行配置已改为本机后端，第三方地图 Key 留空。
- 外部天气、视频、SSO、社交登录、Quartz 和模拟登录在隔离 profile 中默认关闭。
- 已移除包含原环境连接的历史后端 profile 和管理端环境文件。
- 已清理已知生产主机、固定短信验证码、特定手机号白名单和固定注册密码。
- 新项目已初始化独立 Git 仓库，尚未创建基线提交。

## 尚未完成

- 数据库结构、基础数据和模拟数据尚待数据库文件就绪后处理。
- 三套代码尚未在新数据库上完成构建与业务联调。
- 文件存储方式及演示账号需结合数据库初始化确定。

## 重要边界

当前目录是抽离工作副本，不应使用 `dev`、`local`、`test` 或 `prod` profile 启动。只有完成敏感信息扫描、数据库初始化和运行验收后，才能作为最终交付包。
