# 交付安全边界

本项目不得包含或连接原环境的数据库、Redis、对象存储、短信、微信、地图、视频和单点登录凭据。

当前交付版只允许使用 `isolated` profile。历史 `dev`、`local`、`test`、`prod` 环境文件不属于交付内容，因为它们包含原环境连接配置。

第三方能力的实现代码仍然保留，但必须由使用方提供自己的账号，并在独立环境中显式启用。

数据库导出进入 `database/incoming` 后必须先审查，确认不包含生产业务记录、真实用户资料、Token、日志、短信记录、上传文件内容或第三方密钥，才能整理为正式初始化脚本。
